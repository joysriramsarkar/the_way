name: CI & Code Quality

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: Validate Code & Data Integrity
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install Dependencies
      run: npm ci

    - name: Type Check Backend & Frontend (TypeScript)
      run: npm run type-check

    - name: Validate All Web Books JSON Data
      run: |
        node -e "
          const fs = require('fs');
          const path = require('path');
          const booksDir = path.join(__dirname, 'public', 'assets', 'books');
          const files = fs.readdirSync(booksDir).filter(f => f.endsWith('.json'));
          console.log('Validating ' + files.length + ' book JSON files...');
          let hasError = false;
          for (const file of files) {
            const filePath = path.join(booksDir, file);
            try {
              const content = fs.readFileSync(filePath, 'utf8');
              const parsed = JSON.parse(content);
              if (!Array.isArray(parsed)) {
                console.error('❌ ' + file + ' is not a valid JSON array');
                hasError = true;
              } else {
                console.log('✅ ' + file + ' (' + parsed.length + ' chapters/parts)');
              }
            } catch (err) {
              console.error('❌ ' + file + ' JSON parse error: ' + err.message);
              hasError = true;
            }
          }
          if (hasError) process.exit(1);
          console.log('All book data files validated successfully!');
        "

    - name: Run Test Suite
      run: npm test

    - name: Build Next.js Production Application
      run: npm run build

    - name: Test Next.js Production Server Boot
      run: |
        node -e "
          const http = require('http');
          const { exec } = require('child_process');
          console.log('Testing Next.js server bootstrap...');
          const serverProc = exec('npx next start -p 3999', {
            env: { ...process.env, PORT: '3999', SESSION_SECRET: 'test_secret_for_ci_12345678901234' }
          });
          setTimeout(() => {
            http.get('http://localhost:3999', (res) => {
              console.log('Next.js server responded with HTTP code:', res.statusCode);
              serverProc.kill();
              if (res.statusCode === 200) {
                console.log('✅ Next.js server bootstrap verified!');
                process.exit(0);
              } else {
                console.error('❌ Unexpected status code:', res.statusCode);
                process.exit(1);
              }
            }).on('error', (e) => {
              console.error('❌ Server connection error:', e.message);
              serverProc.kill();
              process.exit(1);
            });
          }, 4000);
        "
